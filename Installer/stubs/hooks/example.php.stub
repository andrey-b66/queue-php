<?php
/** @var array $payload Данные вебхука из очереди */

// Файл-пример: обрабатывает вебхуки, пришедшие на ?hook=example
// Имя файла = имя хука. Скопируйте под своим именем и наполните логикой.

error_log('Пришли данные: ' . json_encode($payload, JSON_UNESCAPED_UNICODE));

// ... любая обработка $payload: вызвать свой класс, сходить во внешний API и т.д. ...

// Внимание: не объявляйте const/function на верхнем уровне файла хука —
// за один прогон воркер может подключить его несколько раз.
